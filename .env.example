# Copy to .env and fill in your key. .env is gitignored and excluded from the
# release ZIP, so the key stays local.
#
# Used by the integration suite (composer test:integration), which makes real
# calls to the ElevenLabs API. Tests skip themselves when this is unset.
ELEVENLABS_API_KEY=
